<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <!-- Assembly name is the invoked command, so help usage and the MCP server name read "slnmap". -->
    <AssemblyName>slnmap</AssemblyName>
    <Description>Slnmap (sln-map) — a semantic map of your .sln for AI coding agents. Analyze a .NET solution into a queryable code graph, serve it over MCP, and inspect index status.</Description>
    <PackAsTool>true</PackAsTool>
    <ToolCommandName>slnmap</ToolCommandName>
    <PackageId>Slnmap</PackageId>
    <Version>0.12.1</Version>
    <RollForward>Major</RollForward>
    <PackageReadmeFile>nuget-readme.md</PackageReadmeFile>
    <PackageIcon>icon.png</PackageIcon>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <Authors>Mahmoud Nabil</Authors>
    <Copyright>Copyright (c) 2026 Mahmoud Nabil</Copyright>
    <PackageProjectUrl>https://slnmap.dev</PackageProjectUrl>
    <RepositoryUrl>https://github.com/EMahmoudNabil/slnmap</RepositoryUrl>
    <PackageTags>dotnet;roslyn;mcp;ai;code-analysis;claude-code;knowledge-graph</PackageTags>
    <PackageReleaseNotes>Fix: the cross-stack linker no longer reports false ambiguity between
unrelated, literal-anchored routes. A hole on one side absorbing the other side's literal was
treated as independently excusable regardless of direction, so two unrelated routes could
coincidentally skeleton-match when a call site's hole absorbed one endpoint's literal while
that SAME endpoint's hole absorbed a DIFFERENT call-site literal at another position -- a
coincidence of two unrelated literals hiding behind different holes, not a real relationship
ASP.NET could ever produce. Caught by a pre-launch field trial on a second, larger codebase.
Absorption is now required to run in one direction per comparison; genuine fan-out is
unaffected, verified against the real TaskCenter 3-way fan-out byte-for-byte before and after.
Re-measured on the reference field-trial pair: 24 -> 11 false set-edges resolve to their one
correct endpoint; linked/disclosed totals unchanged (407/434 either way) -- this corrects HOW
call sites resolve, not whether they do. See CHANGELOG.md for details.
Source: https://github.com/EMahmoudNabil/slnmap</PackageReleaseNotes>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\..\docs\nuget-readme.md" Pack="true" PackagePath="\" />
    <None Include="icon.png" Pack="true" PackagePath="\" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Include="Resources\viz-template.html" LogicalName="viz-template.html" />
    <EmbeddedResource Include="Resources\vis-network.min.js" LogicalName="vis-network.min.js" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Slnmap.Analysis\Slnmap.Analysis.csproj" />
    <ProjectReference Include="..\Slnmap.Core\Slnmap.Core.csproj" />
    <ProjectReference Include="..\Slnmap.Mcp\Slnmap.Mcp.csproj" />
    <ProjectReference Include="..\Slnmap.Storage\Slnmap.Storage.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="System.CommandLine" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Slnmap.Tests" />
  </ItemGroup>

</Project>
