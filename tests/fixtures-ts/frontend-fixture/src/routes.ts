// API_ROUTES-style object: a barrel of route-template constants, resolved via the
// checker's literal types at each call site that indexes into it with a literal key.
export const API_ROUTES = {
  userTasksSummary: '/UserTasks/assigned-tasks-with-summary',
};
