@* Fixture .razor file (v0.12.2, foreign-patterns-trial finding #1) — never walked as a Roslyn
   analyzer document; the point is that it's detected via a file-system scan instead. *@
<h3>Sample</h3>
